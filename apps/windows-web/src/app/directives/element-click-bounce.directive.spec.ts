import { ElementClickBounceDirective } from './element-click-bounce.directive';

describe('ElementClickBounceDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementClickBounceDirective();
    expect(directive).toBeTruthy();
  });
});
